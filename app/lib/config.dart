/// App configuration. After deploying the Worker (see ../backend), set this to
/// your own URL, e.g. https://twofa-sync.<your-subdomain>.workers.dev
const String kSyncBaseUrl = 'https://twofa-sync.example.workers.dev';
